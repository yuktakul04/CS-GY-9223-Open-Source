[project]
name = "chat-client-api"
version = "0.1.0"
description = "Abstract interface for chat client components"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chat_client_api"]
