# issue-tracker-integration

Bridges `ospd-issue-tracker-api` with the chat vertical.

`IssueTrackerBridge` wraps the upstream `api.client.Client` and exposes board and issue operations used by the chat service.
