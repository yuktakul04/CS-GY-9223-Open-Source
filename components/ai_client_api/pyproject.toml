[project]
name = "ai-client-api"
version = "0.1.0"
description = "Abstract AI client interface for prompt-based messaging"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_client_api"]
